interface:
  display_name: "Skill Create"
  short_description: "Create evaluation-ready agent skills"
  default_prompt: "Use $skill-create to design an evaluation-ready agent skill from concrete examples."
policy:
  allow_implicit_invocation: true
