interface:
  display_name: "GitHub Create PR"
  short_description: "Create draft GitHub pull requests"
  default_prompt: "Use $gh-create-pr to create a draft pull request for the current branch."
policy:
  allow_implicit_invocation: true
